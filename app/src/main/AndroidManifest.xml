<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arielu.shopper.demo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserMessageBoardActivity" />

        <activity android:name=".BranchProductsActivity" />
        <activity android:name=".BranchesActivity" />
        <activity android:name=".ShoppingListPermissionsActivity" />
        <activity android:name=".SearchItemsActivity" />
        <activity android:name=".UserShoppingListActivity" />
        <activity android:name=".ChooseListActivity" />
        <activity android:name=".UserPanelActivity" />
        <activity android:name=".WorkerPanelActivity" />
        <activity android:name=".MessageBoardActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>